<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="HosipitalManager.MVVM.Views.PatientsContentView"
             xmlns:views="clr-namespace:HospitalManager.MVVM.Views">
   
    <Grid RowDefinitions="Auto, *, Auto">
        <Grid Grid.Row="0" ColumnDefinitions="Auto, *" Margin="0, 0, 0, 15" ColumnSpacing="10">
            <Button Text="+ Thêm Bệnh nhân"
                        Command="{Binding ShowAddPatientPopupCommand}"
                        BackgroundColor="{StaticResource ColorPrimary}"
                        TextColor="White"
                        CornerRadius="8"
                        Padding="15, 10" />

            <SearchBar Grid.Column="1"
                       Placeholder="Tìm theo tên hoặc mã BN..."
                       Text="{Binding SearchText}" 
                       BackgroundColor="White"
                       CancelButtonColor="{StaticResource ColorPrimary}"
                       PlaceholderColor="Gray"
                       TextColor="Black" />
        </Grid>

        <Frame Grid.Row="1"
           Padding="0"
           CornerRadius="12"
           BackgroundColor="White"
           BorderColor="#E0E0E0"
           HasShadow="True"
           VerticalOptions="FillAndExpand">

            <CollectionView ItemsSource="{Binding FilteredPatients}">
                <CollectionView.Header>
                    <Grid Padding="15, 10" BackgroundColor="#F9FAFB" ColumnDefinitions="0.8*, 1.5*, 0.5*, 0.5*, 1.2*, 2*, 1*, 1*">
                        <Label Grid.Column="0" Text="MÃ BN" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="1" Text="HỌ TÊN" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="2" Text="GIỚI TÍNH" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="3" Text="TUỔI" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="4" Text="SĐT" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="5" Text="ĐỊA CHỈ" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="6" Text="TRẠNG THÁI" FontAttributes="Bold" TextColor="#6B7280" />
                        <Label Grid.Column="7" Text="HÀNH ĐỘNG" FontAttributes="Bold" TextColor="#6B7280" />
                    </Grid>
                </CollectionView.Header>

                <CollectionView.ItemTemplate>
                    <DataTemplate>
                        <Grid Padding="15, 10" ColumnDefinitions="0.8*, 1.5*, 0.5*, 0.5*, 1.2*, 2*, 1*, 1*">
                            <Border Grid.ColumnSpan="8" 
                Stroke="#E0E0E0" 
                StrokeThickness="1"
                VerticalOptions="Start"
                Margin="-15, -10, -15, 0"/>

                            <Label Grid.Column="0" TextColor="#6B7280" Text="{Binding Id}" VerticalOptions="Center" />
                            <Label Grid.Column="1" TextColor="#6B7280" Text="{Binding FullName}" FontAttributes="Bold" VerticalOptions="Center" />
                            <Label Grid.Column="2" TextColor="#6B7280" Text="{Binding Gender}" VerticalOptions="Center" />
                            <Label Grid.Column="3" TextColor="#6B7280" Text="{Binding Age}" VerticalOptions="Center" />
                            <Label Grid.Column="4" TextColor="#6B7280" Text="{Binding PhoneNumber}" VerticalOptions="Center" />
                            <Label Grid.Column="5" TextColor="#6B7280" Text="{Binding Address}" VerticalOptions="Center" LineBreakMode="TailTruncation" />

                            <Frame Grid.Column="6" 
                           Padding="8, 4" 
                           CornerRadius="8"
                           HorizontalOptions="Start"
                           VerticalOptions="Center"
                           BorderColor="Transparent"
                           BackgroundColor="Transparent">
                                <Label Text="{Binding Status}" 
                               TextColor= "#6B7280"
                               FontSize="12"
                               FontAttributes="Bold"/>
                            </Frame>

                            <HorizontalStackLayout Grid.Column="7" Spacing="10" VerticalOptions="Center">
                                <ImageButton Source="edit.png" 
                                 WidthRequest="30" 
                                 HeightRequest="30"
                                 Command="{Binding Source={RelativeSource AncestorType={x:Type views:DashboardPageView}}, Path=BindingContext.ShowEditPatientPopupCommand}"
                                 CommandParameter="{Binding .}"/>
                                <ImageButton Source="delete.png" 
                                 WidthRequest="18" 
                                 HeightRequest="18"
                                 Command="{Binding Source={RelativeSource AncestorType={x:Type views:DashboardPageView}}, Path=BindingContext.DeletePatientCommand}"
                                 CommandParameter="{Binding .}" />
                            </HorizontalStackLayout>
                        </Grid>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Frame>
        
        <!--Pagination Controls-->
        <Grid Grid.Row="2" ColumnDefinitions="Auto, *, Auto" Padding="10" BackgroundColor="White">

            <Button Text="&lt; Trước" 
                Grid.Column="0"
                Command="{Binding PreviousPatientPageCommand}"
                IsEnabled="{Binding CanPatientGoBack}"
                BackgroundColor="Transparent" 
                TextColor="{StaticResource ColorPrimary}" 
                BorderColor="{StaticResource ColorPrimary}" 
                BorderWidth="1"/>

            <Label Text="{Binding PatientPageInfo}" 
               Grid.Column="1"
               HorizontalOptions="Center" 
               VerticalOptions="Center" 
               FontAttributes="Bold"/>

            <Button Text="Sau >" 
                Grid.Column="2"
                Command="{Binding NextPatientPageCommand}"
                IsEnabled="{Binding CanPatientGoNext}"
                BackgroundColor="{StaticResource ColorPrimary}" 
                TextColor="White"/>
        </Grid>
        <!--End Pagination Controls-->
    </Grid>

</ContentView>
